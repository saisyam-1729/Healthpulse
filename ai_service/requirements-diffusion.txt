# Additional dependencies for the diffusion subpackage (diffusion/), kept
# separate from requirements.txt per docs/MODEL_SELECTION.md section 5:
# the diffusion code must stay isolated from the existing scikit-learn
# service's dependency footprint. Install both files in the same venv:
#   pip install -r requirements.txt -r requirements-diffusion.txt
torch>=2.2
pyyaml>=6.0
pytest>=7.4
