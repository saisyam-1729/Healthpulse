# Modification Log

Every file added or changed by this project, grouped where a single logical
change spans several files of the same kind. IDs are referenced from
[IMPLEMENTATION_REPORT.md](IMPLEMENTATION_REPORT.md) once written.

| ID | Date | File(s) | Change | Reason | Before | After | Impact |
|----|------|---------|--------|--------|--------|-------|--------|
| MOD-001 | 2026-09-24 | `docs/ARCHITECTURE_AUDIT.md`, `docs/DATA_PIPELINE.md` | Added | Document the actual (code-verified) system architecture and physiological data format before any implementation, per the brief's Phase 1-3 requirement | No architecture/data documentation existed beyond a short README | Two docs grounding every later decision in specific file:line evidence | None on running code; establishes the factual basis for MOD-002 onward |
| MOD-002 | 2026-09-24 | `docs/DIFFUSION_RESEARCH.md`, `docs/MODEL_SELECTION.md` | Added | Research diffusion architectures against HealthPulse's actual constraints and select one, per the brief's Phase 4-7 requirement | No prior model selection existed | Documented decision: CSDI-inspired conditional diffusion, scaled down; SAITS-lite as mandatory baseline; stress-conditioning explicitly excluded pending real HRV data | None on running code; gate before implementation began |
| MOD-003 | 2026-09-24 | `docs/OPEN_QUESTIONS.md` | Added | Surface decisions that need project-owner sign-off rather than assuming them unilaterally | — | A short list (real-data availability, stress scope, pre-existing bug fixes, latency/hosting budget, phasing, cleanup) | None; informational |
| MOD-004 | 2026-09-24 | `ai_service/configs/diffusion.yaml`, `ai_service/configs/diffusion_dev.yaml` | Added | Centralize every model/training/inference hyperparameter in one place, per brief item 21 ("no magic numbers") | No config file existed for this component | Two YAML configs: production-scale and a faster development-scale variant for CPU-only iteration | None on existing app; new files only |
| MOD-005 | 2026-09-24 | `ai_service/diffusion/config.py` | Added | Typed loader/validator for the YAML configs | — | Dataclass-based config with range/consistency validation (e.g. split fractions sum to 1) | None on existing app |
| MOD-006 | 2026-09-24 | `ai_service/diffusion/data/synthetic.py` | Added | Synthetic physiological data generator for development/smoke-testing, per brief item 34 (no real dataset is available — see DATA_PIPELINE.md) | — | Generates correlated HR/SpO2/temp sequences with injected missingness mirroring the real firmware's dropout/gap pattern; explicitly documented as non-clinical | None on existing app |
| MOD-007 | 2026-09-24 | `ai_service/diffusion/data/preprocessing.py` | Added | Reproducible, leak-free preprocessing pipeline, per brief item 10 | — | Sequence-level train/val/test split; normalization fit on train only; configurable windowing; CSDI-style conditioning-mask construction | None on existing app |
| MOD-008 | 2026-09-24 | `ai_service/diffusion/model/embeddings.py`, `denoiser.py`, `scheduler.py`, `csdi.py` | Added | The diffusion model itself: scaled-down CSDI-inspired denoiser + Gaussian diffusion forward/reverse process, per the selection in MODEL_SELECTION.md | No diffusion model existed anywhere in the repo | New `DiffusionModel` class: trainable, checkpointable, samples a predictive distribution (not a point estimate) | None on existing app; new capability only |
| MOD-009 | 2026-09-24 | `ai_service/diffusion/baselines/simple.py`, `attention_imputer.py` | Added | Mandatory non-diffusion baselines, per brief item 14 — the diffusion model must never be evaluated in isolation | — | Persistence, linear-interpolation, mean, and a lightweight SAITS-inspired attention imputer, all sharing the same evaluation harness as the diffusion model | None on existing app |
| MOD-010 | 2026-09-24 | `ai_service/diffusion/evaluation/metrics.py`, `run_eval.py` | Added | Proper evaluation framework, per brief item 15-16 | — | MAE/RMSE/MAPE/sMAPE for all methods; CRPS + interval coverage for the diffusion model; physiological-validity sanity checks (out-of-range / implausible-jump rates) | None on existing app |
| MOD-011 | 2026-09-24 | `ai_service/diffusion/training/train.py` | Added | Reproducible training entrypoint, per brief item 13 | — | Seeded, checkpointed (best + last), resumable, early-stopping, cosine-annealed training loop, config-driven | None on existing app |
| MOD-012 | 2026-09-24 | `ai_service/diffusion/inference/service.py` | Added | Model-resident inference service, per brief item 20 ("do not reload the model for every request") | — | Loads a checkpoint once, keeps it resident; implements impute/forecast/generate/anomaly-score; fails soft (503, not crash) if no checkpoint is trained yet, mirroring the existing `ai_service/app.py` pattern for the scikit-learn models | None on existing app |
| MOD-013 | 2026-09-24 | `ai_service/diffusion/api/routes.py` | Added | New REST endpoints, per brief item 9 (only the endpoints justified by the selected tasks) | — | `/api/diffusion/{impute,forecast,generate,anomaly-score,health}` with input validation and explicit error responses (400 invalid input, 503 model not loaded, 500 inference failure) | None on existing app; new routes only |
| MOD-014 | 2026-09-24 | `ai_service/app.py` | Modified | Register the new blueprint, per the integration decision in MODEL_SELECTION.md (extend the existing service, not a second microservice) | App defined only `/ai/predict` and `/analyze` | Same two routes, plus a defensively try/except-wrapped blueprint registration — if PyTorch/the diffusion package is unavailable, the import failure is caught and logged, the original routes are unaffected | **Verified**: booted the modified app and called both an old route path and the new `/api/diffusion/health` route in the same process; both worked (see IMPLEMENTATION_REPORT.md) |
| MOD-015 | 2026-09-24 | `ai_service/requirements-diffusion.txt` | Added | Keep the diffusion component's dependencies (PyTorch, PyYAML, pytest) isolated from the existing `requirements.txt`, per MODEL_SELECTION.md section 5 | Only scikit-learn/Flask deps existed | Second requirements file, installed alongside the first | None on existing app if not installed (import is defensive, see MOD-014) |
| MOD-016 | 2026-09-24 | `tests/smoke_test_diffusion.py` | Added | Mandatory smoke test, per brief item 33 | No tests existed for any ML component in the repo | End-to-end check: synthetic data → preprocess → tiny model → train → checkpoint save/load → sample → shape/finiteness checks → live in-process Flask API call. **Actually executed and passing** (see IMPLEMENTATION_REPORT.md) | None on existing app |
| MOD-017 | 2026-09-24 | `docs/ARCHITECTURE_BEFORE.md`, `docs/ARCHITECTURE_AFTER.md` | Added | Before/after architecture diagrams, per brief item 25 | — | Two Mermaid diagrams reflecting the actual (not imagined) wiring at each stage | None; documentation only |

| MOD-018 | 2026-09-24 | `ai_service/configs/diffusion_dev.yaml` | Added | Full production config (`diffusion.yaml`) measured at ~84s/epoch — a 100-epoch run would take ~2.3h, too long for this session's turnaround; a reduced, explicitly-labeled dev-scale config was needed to produce real (not projected) results | — | Second config: 200 sequences, 15 epochs, 25 inference sampling steps | None on existing app |
| MOD-019 | 2026-09-24 | `docs/IMPLEMENTATION_REPORT.md`, `README.md` | Added / Modified | Report real measured training and evaluation results, per brief items 15, 37, 38 (no fabricated results) | README had no ML section | Dev-scale run (15 epochs) reported honestly as underperforming baselines; see MOD-020 for the subsequent full-scale run | None on running code; documentation only |
| MOD-020 | 2026-09-24 | `docs/IMPLEMENTATION_REPORT.md`, `README.md`, `ai_service/checkpoints/*` | Modified / Added | Full production-scale training run (`configs/diffusion.yaml`, 100 epochs) actually executed (~2h15m, CPU) and evaluated against the same baselines on the same held-out windows, superseding the dev-scale numbers | Dev-scale results showed the model underperforming baselines | Full-scale results: diffusion statistically ties the attention-imputer baseline on MAE/RMSE (0.268 vs 0.269) and clearly beats persistence/interpolation, while providing calibrated uncertainty (82.4% coverage at 90% target, up from 45.6%) that no baseline can produce — reported with the caveat that accuracy parity, not superiority, is the honest characterization | None on existing app; new checkpoint artifacts + updated docs only |

**No file belonging to `backend/`, `src/`, `esp32_ble_firmware/`,
`esp32_health_monitor/`, or `supabase/` was modified in this phase.** The
existing `ai_service/app.py` scikit-learn endpoints were verified still
functional after MOD-014 (see IMPLEMENTATION_REPORT.md "Validation
checklist").
